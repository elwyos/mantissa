//////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////
#bodinc basegame mantissa consi.logger
//////////////////////////////////////////////////////////////////////////////////////////
#bodinc consolearg roengl.neocommando
//////////////////////////////////////////////////////////////////////////////////////////
// dasq.packages = roengl basegame mars field cgl smallmap isomodels modelsandstuff mapgens tsys sqlite
// dasq.libs = png curl
//////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////
[body]
   BG::Starter starter([]{
      
      using namespace Mantissa;
      
      auto& bg = BG::bg();
      
      BG::no3d(); // screw that aye
      
      //BG::enableDither(22, 2);
      BG::disableDither();
      //BG::postrenderShasp("celtest");
      
      //BG::mk().flatground(0, -90);
      //BG::mk().flatground(0, -0.01f);
      
      manti();
      
      BG::touchhandlers / [](const TouchInfo& tinfoil){
         return manti->handleTouchEvent(tinfoil);
      };
      
      BG::drawer2D / []{
         manti->drawPost2D();
      };
      
      BG::updaters / []{
         manti->update();
      };
      
      BG::postparticlers / []{
      };
      
      
      Roen::neoc("system") / []{
             
         // - - - - - - - - - - - - - - - - - - - - - - - - - - - -
         Roen::neocruler();
         
         Roen::neoc("hello") / []{
            CALLconslog(hello, uist() / 196_xcoel / "hello was called");
         };
         
         Roen::neoc("sprites") / []{
            
            // okay, let's see eh?
            
         };
         
         BG::marsiso().enableNeocMenu();
         
         // do something with some neocs
         
         // - - - - - - - - - - - - - - - - - - - - - - - - - - - -
         
      };
      
      
   });
   
   

